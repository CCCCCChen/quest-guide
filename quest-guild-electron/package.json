{
  "name": "quest-guild",
  "version": "1.0.0",
  "description": "悬赏任务公会 - RPG游戏风格的目标管理工具",
  "main": "main.js",
  "author": "Quest Guild",
  "license": "MIT",
  "scripts": {
    "start": "npm run build:renderer && electron .",
    "build:renderer": "node -e \"const cp=require('child_process');const fs=require('fs');const path=require('path');process.chdir('..');const r=cp.spawnSync(process.execPath,['node_modules/vite/bin/vite.js','build','--outDir','quest-guild-electron/renderer/app','--emptyOutDir'],{stdio:'inherit'});if(r.status!==0){process.exit(r.status||1);}const file=path.join(process.cwd(),'quest-guild-electron/renderer/app/index.html');let html=fs.readFileSync(file,'utf8');const LT=String.fromCharCode(60);const GT=String.fromCharCode(62);const closeScript=LT+'/script'+GT;const dropKeywords=['KSlardarWeb','slardarScript','teaScript','collectEvent','apaas_miaoda','miaoda-sdk','bytescm','feishucdn','bytedapm','performance.iife.js','window.appId'];let out='';let i=0;while(true){const start=html.indexOf(LT+'script',i);if(start===-1){out+=html.slice(i);break;}const openEnd=html.indexOf(GT,start);if(openEnd===-1){out+=html.slice(i);break;}const end=html.indexOf(closeScript,openEnd+1);if(end===-1){out+=html.slice(i);break;}const block=html.slice(start,end+closeScript.length);const shouldDrop=block.includes('http://')||block.includes('https://')||dropKeywords.some(k=>block.includes(k));out+=html.slice(i,start);if(!shouldDrop)out+=block;i=end+closeScript.length;}html=out;out='';i=0;while(true){const start=html.indexOf(LT+'link',i);if(start===-1){out+=html.slice(i);break;}const end=html.indexOf(GT,start);if(end===-1){out+=html.slice(i);break;}const tag=html.slice(start,end+1);const shouldDrop=tag.includes('http://')||tag.includes('https://');out+=html.slice(i,start);if(!shouldDrop)out+=tag;i=end+1;}html=out;const appName='\\u60ac\\u8d4f\\u4efb\\u52a1\\u516c\\u4f1a';const appDesc=appName+' - RPG\\u6e38\\u620f\\u98ce\\u683c\\u7684\\u76ee\\u6807\\u7ba1\\u7406\\u5de5\\u5177';html=html.replace(/\\{\\{\\{appAvatar\\}\\}\\}/g,'./favicon.svg').replace(/\\{\\{appAvatar\\}\\}/g,'./favicon.svg').replace(/\\{\\{appName\\}\\}/g,appName).replace(/\\{\\{appDescription\\}\\}/g,appDesc);html=html.replace(/\\{\\{[^}]+\\}\\}/g,'');fs.writeFileSync(file,html);\"",
    "prebuild": "node -e \"const cp=require('child_process');try{cp.execSync('rd /s /q release',{cwd:__dirname,stdio:'pipe'});console.log('已清理 release 目录');}catch(e){if(!e.message.includes('not'))console.log('清理跳过:',e.message.trim())}\"",
    "retry-build": "node -e \"const cp=require('child_process');const p=require('path');const eb=p.join(__dirname,'node_modules','.bin','electron-builder.cmd');const args=process.argv.slice(2);for(let i=0;i<3;i++){const r=cp.spawnSync(eb,args,{cwd:__dirname,stdio:'inherit',shell:true,env:{...process.env,ELECTRON_BUILDER_CACHE:p.join(__dirname,'.electron-cache')}});if(r.status===0)process.exit(0);console.log('尝试 '+(i+1)+' 失败，'+(i<2?'3秒后重试...':'已达最大重试次数'));if(i<2)cp.execSync('timeout /t 3 /nobreak',{stdio:'inherit'});}process.exit(1);\" --",
    "build": "npm run prebuild && npm run build:renderer && npm run retry-build",
    "build:win": "npm run prebuild && npm run build:renderer && npm run retry-build -- --win",
    "build:win64": "npm run prebuild && npm run build:renderer && npm run retry-build -- --win --x64",
    "build:portable": "npm run prebuild && npm run build:renderer && npm run retry-build -- --win portable"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1"
  },
  "dependencies": {
    "electron-store": "^8.1.0"
  },
  "build": {
    "appId": "com.questguild.app",
    "productName": "悬赏任务公会",
    "electronDownload": {
      "cache": ".electron-cache"
    },
    "directories": {
      "output": "release"
    },
    "files": [
      "main.js",
      "preload.js",
      "renderer/**/*",
      "assets/**/*"
    ],
    "win": {
      "signAndEditExecutable": false,
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        },
        {
          "target": "portable",
          "arch": ["x64"]
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "悬赏任务公会"
    },
    "portable": {
      "artifactName": "悬赏任务公会-Portable.exe"
    }
  }
}
