# 悬赏任务公会 (Quest Guild) — 系统规格书 V3.0（基于实际实现）

> 本文档为对当前仓库代码的「如实描述（as-built）」规格书，反映已落地的功能与实现细节，而非早期规划意图。原始规划意图见历史版本 V2.0。

## 1. 概述

**产品名称**：悬赏任务公会
**当前版本**：v2.0（前端标识）/ 后端 v1.0 / 文档规格 V3.0
**核心定位**：以 Dark Fantasy RPG 面板为视觉风格、LLM 驱动任务拆解、结合专注追踪与全局技能树的游戏化目标管理工具。
**目标用户**：希望用游戏化方式克服拖延、管理复杂长期目标，同时能处理日常零碎任务的个人用户。

**实际交付形态（三层架构）**：
1. **Web 前端**（`src/`，Vite + React 18 + TypeScript）：响应式网页应用，可独立运行（纯 localStorage 模式）。
2. **Electron 桌面壳**（`quest-guild-electron/`）：将 Web 前端打包为 Windows 桌面应用，新增悬浮窗、系统托盘、加密本地存储。
3. **可选后端服务**（`server/`，Express + SQLite）：提供持久化数据库、AI 任务拆解能力；Web/Electron 前端可通过 API 客户端与之对接。

---

## 2. 平台与架构

### 2.1 技术栈（如实现）

| 层 | 技术 | 说明 |
|:---|:---|:---|
| 前端 | React 18 + TypeScript + Vite + Tailwind + shadcn/ui + Radix | 6 个页面、路由导航、framer-motion 动效 |
| 图表 | echarts-for-react | 统计页柱状图/折线图/环形图 |
| 状态层 | 自定义 hooks（useTasks/useSkills/useShop/useStats/useSettings/useFloatingMode） | 基于 localStorage 的读写封装 |
| 桌面壳 | Electron 28 + electron-store（加密 JSON） | 主窗口 + 独立悬浮窗 + 系统托盘 |
| 后端 | Express 4 + better-sqlite3（WAL 模式）| RESTful API，30+ 端点 |
| LLM | OpenAI 兼容接口（Ollama / 火山方舟豆包）| 任务拆解，强制 JSON 输出 |

### 2.2 平台策略（当前实现）
- **首发平台**：Windows 桌面（Electron）。前端为响应式 Web，亦可浏览器直接运行。
- **移动端**：未实现独立移动端；Web 响应式可在手机浏览器查看，但悬浮窗/托盘为桌面专有。
- **离线可用**：前端纯 localStorage 模式下完全离线可用；Electron 桌面壳离线可用（含加密存储）；后端服务离线时 AI 拆解不可用，其余核心功能仍由前端本地存储承载。

### 2.3 非功能特性（已落地）
- 毛玻璃顶部状态栏（`bg-card/80 backdrop-blur-xl`）、金紫双主色体系、符文角饰装饰。
- 悬浮窗：收起/展开两态、拖拽移动、位置记忆、透明度与收起延迟可配置。
- 系统托盘常驻；主窗口关闭按钮默认最小化到悬浮窗而非退出。
- 数据加密：Electron 端 `electron-store` 以硬编码 key `quest-guild-2024` 加密。

---

## 3. 游戏化核心概念（已落地）

| 游戏术语 | 映射现实 | 实现状态 |
| :--- | :--- | :--- |
| **史诗任务** | 长期/复杂目标 | ✅ 树形结构 + 父子层级 + Boss 进度条 |
| **每日悬赏** | 日常例行/临时小任务 | ✅ 卡片网格 + 快捷录入 |
| **任务Boss** | 目标最终检验成果 | ✅ epic 任务专属 bossName + bossProgress；完成时自动设为 100 |
| **法力水晶** | 注意力上限 | ✅ 顶部状态栏钻石阵列可视化 + settings.manaMax 可调（1-10） |
| **追踪任务** | 唯一主线专注任务 | ✅ 同时仅 1 个、计时器、开始/暂停/完成 |
| **注意力任务** | 可并行次级任务 | ✅ 受 manaMax 限制的激活校验 |
| **技能树** | 全局能力图谱 | ✅ SVG 画布、拖拽平移、滚轮缩放、5 大类预设框架 |
| **公会声望** | 积分 | ✅ 完成任务/击败 Boss/兑换商品动态结算 |
| **艾宾浩斯回顾** | 遗忘曲线复习 | ❌ **未实现**（见第 8 节） |
| **阶段(stage)** | 子任务分组 | ✅ 子任务按 stage 分组渲染，同阶段并行、跨阶段串行 |

---

## 4. 页面与功能实现清单

> 路由：`/`（今日任务）、`/quest-pool`、`/skill-tree`、`/shop`、`/stats`、`/settings`。左侧导航 + 顶部状态栏 + 主内容区三栏式。

### 4.1 全局组件

**AppSidebar**（左侧导航，可折叠为图标模式）
- Header：金紫渐变 logo + "悬赏任务公会 / Quest Guild · 冒险者大厅"
- 6 个导航项（lucide 图标）：今日任务(Scroll)、悬赏池(Swords)、技能树(TreePine)、公会商店(Store)、统计面板(BarChart3)、设置(Settings)
- Footer："愿勇气与你同在 · v2.0 · 冒险者大厅"

**Topbar**（顶部状态栏，sticky + 毛玻璃）
- 左：双行标题 "悬赏任务公会 / QUEST GUILD"
- 右四区：①最小化到悬浮窗按钮 ②法力水晶（钻石阵列 used/max）③公会声望（金色数字）④今日专注时长

**FloatingWidget**（悬浮窗组件，同时用于浏览器内固定定位与 Electron 独立窗口）
- 收起态（360×72）：追踪任务名 + 计时器 + 今日完成数 + 法力数；可拖拽；透明度/收起延迟由 settings 控制
- 展开态（400×420 可滚动）：头部（置顶/收起/展开主界面按钮）+ 追踪任务卡（难度/Boss 进度条/计时/开始暂停完成）+ 注意力任务列表 + 快速悬赏表单 + 底部状态条
- Electron 集成：调用 `electronAPI.window.expandFloat/collapseFloat/showMain`；`WebkitAppRegion` 区分拖拽区与可点击区

### 4.2 今日任务页（`/`）
- 追踪任务区：大号卡片，左侧难度/预估/Boss 进度，右侧大号计时器（`text-5xl`）+ 开始/暂停/完成；无追踪任务时显示 pending 快捷激活
- 注意力任务区：1/2/3 列响应式网格；点击完成触发粒子爆裂动画（8 点径向扩散 400ms）后回调
- 待激活悬赏区：pending 任务卡片，"设为追踪"/"加入注意力"按钮
- 快速悬赏 Dialog：任务名 + 预估分钟 + 难度 + 类型（每日/史诗）→ 自动加入注意力任务
- 完成确认 AlertDialog + 声望结算（`max(10, round(分钟 × 难度系数))`，easy=0.5/normal=1/hard=2/epic=3）
- 法力校验：`activeAttentionTasks.length >= manaMax` 时 toast 报错

### 4.3 悬赏池页（`/quest-pool`）
- Tabs：史诗任务（树形）/ 每日悬赏（网格）
- 筛选器：搜索（name+description）+ 难度 + 状态
- **EpicTaskNode**：可展开/收起树节点；难度/Boss badge；子任务进度 `completedCount/total`；Boss 血量条（motion 动画）；追踪/激活/编辑/删除按钮
- **子任务区**：按 `stage` 分组，阶段间箭头分隔，每阶段显示"阶段 N · 可并行/顺序"badge + 总分钟数；**AI 拆解按钮**调用 `tasksApi.decompose`
- **DailyTaskCard**：完成按钮 + 名称 + 难度 + 描述 + 预估/声望 + 状态 badges + 操作按钮组
- 创建/编辑 Dialog（react-hook-form + zod）：名称(1-50)、描述(≤200)、类型、难度、预估分钟(5-9999)、关联技能、Boss 名称(仅 epic)、父任务(仅 epic)
- 删除确认：epic 提示"及其所有子任务"
- 完成结算：更新 status + `addReputation` + 若有 `rewardSkillPoints` 则 `addSkillPoints`

### 4.4 技能树页（`/skill-tree`）
- SVG 画布（高 600px）：右上角缩放控制（放大/缩小/重置）、左上角图例（已强化金/已点亮紫/未解锁灰）
- 背景：3 同心圆符文圈；金色径向渐变 + Gaussian blur 滤镜
- 连线：激活态实线 + 类别色，部分激活灰色虚线，未激活暗色虚线
- 节点：外发光圈（motion 呼吸动画）+ 外圈 + 主体圆 + 图标（foreignObject 嵌 lucide）+ 名称（level 0/1 显示）+ 强化标记（金色 ★）
- 节点大小：level 0=36 / level 1=28 / level 2=22
- **交互**：拖拽平移（按 `viewBox.w/clientWidth` 比例换算）；滚轮缩放（deltaY 决定 1.15/0.87 倍，viewBox clamp 400-3000 / 320-2400，以鼠标为中心）；点击节点不触发拖拽
- 节点详情侧栏（w-80）：图标 + 名称 + 类别/状态 badge + 描述；locked 态显示解锁所需/可用技能点 + 解锁按钮；unlocked 态显示强化按钮；enhanced 态显示金边卡片；关联史诗任务列表（带各自 Boss 进度条）
- 5 大类（图标+颜色）：编程(Code2/#3BA3E0)、设计(Palette/#9B59E6)、语言(BookOpen/#4CAF7A)、商业(Briefcase/#E8B34A)、生活技能(Home/#F29940)
- 解锁校验：状态须 locked + 父节点非 locked + `skillPoints >= requiredSkillPoints`
- 底部 5 卡统计：总技能/已点亮/已强化/技能分类数/可用技能点

### 4.5 公会商店页（`/shop`）
- 页头：图标 + "公会商店" + 当前声望胶囊 + "添加商品"按钮
- Tabs：商品列表 / 已兑换记录（带数量 badge）
- 商品网格 1/2/3/4 列响应式：渐变背景（按 color 选 6 色系）、圆形 emoji 图标、名称、描述、所需声望、兑换按钮（可兑换金色 / 不足灰色 disabled）；自定义商品 hover 显示编辑/删除
- 筛选：全部 / 预设(mock) / 自定义(user)
- 兑换逻辑：`reputation < cost` toast 报错，否则 `redeemItem`
- 兑换记录：倒序列表，每行成功图标 + 商品名 + 时间 + 消耗声望（红色负数）
- 添加/编辑 Dialog：名称、积分(min=1)、图标(15 emoji 选项)、颜色(6 色板：gold/purple/green/blue/red/orange)、描述

### 4.6 统计面板页（`/stats`）
- KPI 卡片网格 4 张：本周专注时长(Clock/primary)、本周完成任务数(CheckCircle2/success)、连续完成天数(Flame/warning)、累计声望(Star/accent)
- 图表区第一行：近 7 天完成任务柱状图（金色渐变柱，echarts）/ 难度分布环形图（4 段）
- 图表区第二行：近 7 天专注时长折线图（紫色平滑曲线 + 渐变面积）/ 技能成长卡片（已点亮/可用技能点 + 最近完成 5 条任务列表）
- 统一 RPG 配色：tooltip 深色背景、金/紫坐标轴

### 4.7 设置页（`/settings`）
- **法力水晶上限**：可视化水晶阵列 + Slider(1-10)
- **每日重置时间**：`<Input type="time">`
- **音效反馈**：Switch，开启时 `playSound('levelup')`
- **悬浮窗设置**：透明度 Slider(0.4-1) + 收起延迟 Slider(300-3000ms) + 重置悬浮窗位置按钮
- **数据管理**（功能最丰富）：
  - 存储空间 Progress 条（`getStorageUsage()`，≥80% 琥珀色警告）
  - 三按钮：导出 JSON（`quest-guild-backup-YYYY-MM-DD.json` 下载）/ 导入 / 立即备份
  - 备份记录列表（每日备份/操作备份 badge + 时间 + 任务数 + 字节数 + 恢复按钮）
  - 恢复确认 AlertDialog（列出备份详情 + "当前数据将被完全覆盖"警告）
  - 导入预览弹窗（校验结果 + 4 格预览 + 导入模式 Tabs：覆盖模式 / 合并模式按 id 去重）
  - 重置所有数据（destructive，AlertDialog 二次确认，清空 9 个 localStorage 键 + 重置 + 1s 刷新）
- **关于**：版本 2.0 · 冒险者大厅 + 产品说明

---

## 5. 数据架构

### 5.1 双存储模式

前端支持两种存储后端，通过 `window.isElectron` 与 API 客户端判断切换：

**模式 A — 纯 localStorage（Web 浏览器或离线）**
- 9 个 `__quest_guild_*` 键，JSON 序列化
- 首次加载注入 source='mock' 的预设示例数据
- 适合无后端的纯浏览器场景

**模式 B — Electron 加密存储 + 后端 SQLite（桌面版）**
- Electron 主进程 `electron-store` 加密 JSON（`quest-guild-data.json`，key=`quest-guild-2024`，存于 `%APPDATA%/quest-guild/`）
- 通过 `preload.js` 暴露 `window.electronAPI.store.*`（get/set/delete/getAll/clear）
- 存储变更通过 `data:storage-changed` 广播到所有窗口
- 后端 SQLite（`quest-guild.db`，WAL 模式）通过 `api:proxy` IPC 通道转发到 `localhost:3001/api/*`

### 5.2 localStorage 存储键（9 个）

| 键 | 类型 | 用途 |
|:---|:---|:---|
| `__quest_guild_tasks` | `IQuestTask[]` | 全部任务 |
| `__quest_guild_skills` | `ISkillNode[]` | 技能树节点 |
| `__quest_guild_skill_points` | `number` | 可用技能点 |
| `__quest_guild_shop_items` | `IShopItem[]` | 商店商品 |
| `__quest_guild_reputation` | `number` | 公会声望 |
| `__quest_guild_redemptions` | `IRedemption[]` | 兑换记录 |
| `__quest_guild_active_track` | `IActiveTrack \| null` | 追踪任务状态 |
| `__quest_guild_focus_log` | `IFocusLog[]` | 专注记录 |
| `__quest_guild_settings` | `IAppSettings` | 用户设置 |

### 5.3 核心数据接口（`src/types/quest.ts`，如实现）

```ts
export type TaskType = 'epic' | 'daily';
export type TaskDifficulty = 'easy' | 'normal' | 'hard' | 'epic';
export type TaskStatus = 'pending' | 'active' | 'completed';
export type SkillStatus = 'locked' | 'unlocked' | 'enhanced';

export interface IQuestTask {
  id: string;
  name: string;
  description: string;
  type: TaskType;
  difficulty: TaskDifficulty;
  estimatedMinutes: number;
  actualMinutes: number;
  relatedSkillId?: string;
  bossName?: string;            // 史诗任务特有
  bossProgress: number;        // 0-100
  parentId?: string;           // 史诗子任务父ID
  stage: number;               // 阶段编号，同阶段并行、跨阶段串行
  status: TaskStatus;
  isTracking: boolean;         // 是否为追踪任务
  isAttention: boolean;        // 是否为注意力任务
  rewardReputation: number;
  rewardSkillPoints: number;
  createdAt: number;
  completedAt?: number;
  source?: 'mock' | 'user';
  tags?: string[];
}

export interface ISkillNode {
  id: string;
  name: string;
  description: string;
  category: string;            // 编程/设计/语言/商业/生活技能
  parentId?: string;
  level: number;               // 0=根节点
  status: SkillStatus;
  x: number;                   // 画布坐标（预计算）
  y: number;
  icon?: string;
  requiredSkillPoints: number;
  unlockedAt?: number;
}

export interface IShopItem {
  id: string;
  name: string;
  cost: number;
  icon: string;                // emoji
  color?: string;              // gold/purple/green/blue/red/orange
  description?: string;
  source: 'mock' | 'user';
  createdAt: number;
}

export interface IManaConfig { maxAttentionTasks: number; current: number; }
export interface IActiveTrack { taskId: string; accumulatedSeconds: number; lastStartTime?: number; isRunning: boolean; }
export interface IFocusLog { id: string; taskId: string; date: string; durationMinutes: number; completedAt: number; }
export interface IRedemption { id: string; itemId: string; itemName: string; cost: number; redeemedAt: number; }
export interface IFloatingPosition { x: number; y: number; }

export interface IAppSettings {
  manaMax: number;
  resetTime: string;          // HH:mm
  soundEnabled: boolean;
  floatingPosition: IFloatingPosition;
  floatingOpacity: number;
  floatingCollapseDelay: number;
}
```

---

## 6. Electron 桌面版（`quest-guild-electron/`）

### 6.1 主进程（`main.js`，487 行）

**窗口管理**：
- 主窗口 `createMainWindow()`：1280×800（min 1024×680），frame:true，背景 `#1a1a2e`；`close` 事件被拦截为 `hide()` + 显示悬浮窗（关闭即最小化到悬浮窗）
- 悬浮窗 `createFloatWindow()`：360×72（收起）/ 360×500（展开），frame:false，transparent:true，alwaysOnTop:true，skipTaskbar:true；位置记忆于 `store.floatWindowPosition`；`setShape` 仅留顶部 360×72 可点击（收起态鼠标穿透）
- 缩放锚点为底边（`resizeFloatWindowAnchored` 保持底边不动向上扩展）；展开/收起动画 180ms cubic ease-out（16ms/帧）

**系统托盘**（`createTray()`）：
- 图标 `assets/icon.png`（磁盘不存在则回退 `nativeImage.createEmpty()`）
- 右键菜单：打开主界面 / 显示悬浮窗 / （分隔）/ 今日任务概览（占位 disabled）/ （分隔）/ 退出
- 单击托盘：主窗口与悬浮窗快速切换

**IPC 处理器**：
- 存储：`store:get` / `store:get-sync` / `store:set`（+ 广播变更）/ `store:delete` / `store:getAll` / `store:getAll-sync` / `store:clear`
- 窗口：`window:showMain` / `window:hideMain` / `window:minimizeToFloat` / `window:expandFloat` / `window:collapseFloat` / `app:quit`
- API 代理：`api:proxy` 接收 `{method, path, body}`，用 Node 原生 `http.request` 转发到 `localhost:3001/api/*`，返回 `{status, body}` 或 `{status:0, error}`
- 数据同步：`syncDataToRenderer()` 在主窗口加载完成时发送全量 store 数据；`broadcastStorageChange` 向所有窗口广播变更

**生命周期**：`whenReady` 创建主窗口+托盘，1 秒后自动显示悬浮窗；`window-all-closed` 非 macOS 不退出（保持托盘）；`before-quit` 设 isQuitting=true。

### 6.2 预加载脚本（`preload.js`）

通过 `contextBridge.exposeInMainWorld('electronAPI', {...})` 暴露：
- `store.get/set/delete/getAll/clear`
- `window.showMain/hideMain/minimizeToFloat/expandFloat/collapseFloat/quit`
- `onDataSync(callback)` — 监听全量 store 数据
- `onStorageChanged(callback)` — 监听 `{key, value, timestamp}`，返回 unsubscribe
- `api.proxy(method, path, body)` — 调用后端
- 另暴露 `window.isElectron = true`

类型定义见 `src/types/electron.d.ts`。

### 6.3 悬浮窗页面（`renderer/float.html`，797 行纯静态）

- **性质**：完全脱网的单文件 HTML（内联 CSS + 内联 JS），无任何外部资源
- **收起态**（`.float-bar`，360×72）：星形 logo + 任务名 + 计时器 + 法力水晶点阵 + 展开按钮；`-webkit-app-region: drag` 可拖拽
- **展开态**（`.expanded-panel`，360×500）：头部 + 追踪任务卡（Boss 进度条 + 大号计时器 + 开始/完成按钮）+ 5 项注意力列表 + 底部快捷栏（快速悬赏/技能树/公会商店，占位无逻辑）
- **JS 常量**：`EXPAND_DELAY=180ms` / `COLLAPSE_DELAY=260ms` / `WINDOW_EXPAND_DELAY=28ms` / `WINDOW_COLLAPSE_DELAY=150ms`
- **计时器**：从 `seconds=2730`（45:30）开始 `setInterval` +1，纯前端 demo
- **关键限制**：所有任务名/计时/Boss 进度/注意力列表均为**硬编码静态 demo 数据**，未对接 `electronAPI.store`，未监听 `data:sync` / `data:storage-changed`。即悬浮窗独立页面为 UI 原型，与主窗口数据双向同步尚未实现

> 注：主窗口内的 `<FloatingWidget />` React 组件（在 `src/components/FloatingWidget.tsx`）才是数据实时同步的版本；`float.html` 是 Electron 独立悬浮窗页面的静态占位实现。

### 6.4 ⚠️ 已知阻塞问题

`quest-guild-electron/package.json` **当前为空文件（0 字节）**。这导致：
- `npm install` 在该目录无法安装 electron / electron-store / electron-builder
- AGENTS.md 中描述的 `start` / `build:win64` / `build:renderer` 脚本、electron-builder 配置（productName、directories.output=release、win target、nsis 设置、files 列表）**当前均不在该文件中**
- 同目录 `package-lock.json` 仍存在，可据此恢复依赖版本
- 同目录有 `安装并运行.bat` / `打包成EXE.bat` 批处理脚本

---

## 7. 后端服务（`server/`）

### 7.1 概述
- 框架：Express 4 + cors + better-sqlite3
- 端口：`process.env.PORT || 3001`
- 数据库：`quest-guild.db`，`journal_mode=WAL`
- 无 dotenv 依赖（自实现简易 .env 解析器，且不覆盖已存在的 process.env）
- 无 axios/fetch 库（用 Node 18+ 内置 fetch）
- `dev` 脚本用 `node --watch` 热重载

### 7.2 数据表结构
1. **tasks** — id, name, description, type, difficulty, estimatedMinutes, actualMinutes, relatedSkillId, bossName, bossProgress, parentId, **stage**(DEFAULT 1), status(DEFAULT 'pending'), isTracking, isAttention, rewardReputation, rewardSkillPoints, createdAt, completedAt, source(DEFAULT 'user'), tags(JSON)
2. **active_track** — 单例（`CHECK(id=1)`），taskId, accumulatedSeconds, lastStartTime, isRunning
3. **focus_logs** — id, taskId, date, durationMinutes, completedAt
4. **skills** — id, name, description, category, parentId, level, status(DEFAULT 'locked'), x, y, icon, requiredSkillPoints, unlockedAt
5. **shop_items** — id, name, cost, icon, color, description, source(DEFAULT 'user'), createdAt
6. **redemptions** — id, itemId, itemName, cost, redeemedAt
7. **meta** — key/value 键值对（settings、reputation、skill_points 等）

### 7.3 默认元数据初始化
- `skill_points = 3`
- `reputation = 100`
- `settings = { manaMax:5, resetTime:'06:00', soundEnabled:true, floatingPosition:{x:-24,y:-24}, floatingOpacity:0.8, floatingCollapseDelay:1000 }`

### 7.4 API 端点清单（全部 `/api` 前缀）

**任务**
| Method & Path | 行为 |
|:---|:---|
| `GET /api/tasks` | 全部任务（按 createdAt DESC） |
| `POST /api/tasks` | 创建；自动计算奖励：`rewardReputation = {easy:5,normal:10,hard:25,epic:80}[d] + floor(estimatedMinutes/10)*2`；`rewardSkillPoints = {easy:0,normal:0,hard:1,epic:3}[d]`；id 前缀 epic→`ep_` / daily→`dy_` |
| `PATCH /api/tasks/:id` | 动态 SET 更新（过滤 id 字段） |
| `DELETE /api/tasks/:id` | 删除自身 + 所有 parentId=id 的子任务；若为当前追踪任务则清空 active_track |
| `POST /api/tasks/:id/complete` | 完整结算：(1) 追踪任务结算实际秒数并写入 focus_logs (2) 清空 active_track (3) 任务 status=completed、isTracking/isAttention=0、epic bossProgress=100 (4) **史诗级联**：若有 parentId，检查兄弟任务全部完成→父任务 completed+bossProgress=100，否则按完成数/总数更新父 bossProgress (5) +reputation (6) 若 rewardSkillPoints>0 则 +skill_points |
| `POST /api/tasks/decompose` | AI 拆解：接收 `{name, description, difficulty, type, estimatedMinutes}`，调用 `services/llm.js`，返回 `{subtasks}` |

**追踪任务**
| `GET /api/active-track` | 返回单例（无则插入空记录后返回 null） |
| `POST /api/active-track` | body `{taskId}`；先清所有 isTracking；若 taskId 空：结算当前 running 累计并清空；若非空：从注意力移除、设 isTracking=1、status='active'，保留累计时间 |
| `POST /api/active-track/start` | isRunning=1, lastStartTime=now；无追踪任务返回 400 |
| `POST /api/active-track/pause` | elapsed += floor((now-lastStartTime)/1000)，isRunning=0 |

**注意力任务**
| `POST /api/tasks/:id/attention` | body `{maxAttention}`；校验：非追踪 + 当前未完成注意力数 < maxAttention |
| `DELETE /api/tasks/:id/attention` | isAttention=0 |

**专注日志**：`GET /api/focus-logs`（按 completedAt DESC）

**设置**：`GET /api/settings` / `PATCH /api/settings`（浅合并）

**技能**
| `GET /api/skills` | 按 category, level 排序 |
| `POST /api/skills/batch-init` | 事务批量 INSERT OR IGNORE |
| `POST /api/skills` / `PUT /api/skills/:id` / `DELETE /api/skills/:id` | CRUD（PUT 字段白名单） |
| `POST /api/skills/import` | 覆盖式批量导入（先 DELETE 再事务插入） |
| `GET /api/skill-points` | 读 meta.skill_points |
| `POST /api/skills/:id/unlock` | 校验：locked + `skill_points >= requiredSkillPoints` + **父节点已解锁**；扣除技能点、设 unlocked、记录 unlockedAt |
| `POST /api/skills/:id/enhance` | 校验：unlocked；**强化成本 = ceil(requiredSkillPoints × 1.5)**；扣除、设 enhanced |

**商店**
| `GET /api/shop/items` | 按 createdAt ASC |
| `POST /api/shop/items` | 新建（id `shop_${ts}_${random5}`, source='user'） |
| `DELETE /api/shop/items/:id` | **禁止删除 source='mock' 预设商品**（返回 400） |
| `GET /api/shop/reputation` / `POST /api/shop/reputation` | 读/写 meta.reputation（`max(0, current + amount)` 支持负数） |
| `GET /api/shop/redemptions` | 按 redeemedAt DESC |
| `POST /api/shop/redeem/:itemId` | 校验：存在 + reputation>=cost；扣除、创建 redemption 记录 |

**其他**：`POST /api/bootstrap`（幂等批量初始化）、`GET /api/health`

### 7.5 LLM 服务（`server/services/llm.js`，132 行）

**支持的供应商**（由 `process.env.LLM_PROVIDER` 切换）：
- `ollama`（默认）：baseURL `http://localhost:11434/v1`，model `qwen2:7b`
- `doubao`（火山方舟 Ark）：baseURL `https://ark.cn-beijing.volces.com/api/v3`，model `doubao-pro-32k`

均使用 OpenAI 兼容 `/chat/completions` 端点，`temperature=0.7`，`response_format={type:'json_object'}` 强制 JSON 输出。

**System Prompt 要点**：
- 角色"资深项目管理专家"
- 拆成 4-10 个子任务，按 stage 分组（2-5 个 stage，同 stage 并行、跨 stage 串行）
- 颗粒度 20-120 分钟，动词开头、可执行、独立可交付
- 难度对齐时间：easy≈25 / normal≈45 / hard≈75 / epic≈100
- **强调最小 20 分钟，禁止 5/10/15 分钟估算**
- 输出纯 JSON `{ "subtasks": [{name, description, difficulty, estimatedMinutes, stage}] }`

**容错解析**：先 `JSON.parse`，失败则正则 `/\{[\s\S]*\}/` 提取首个 JSON 块再 parse，仍失败抛错（截断前 200 字符）。

**导出**：`decomposeTask(task)` 接收 `{name, description, difficulty, type, estimatedMinutes}`，构造 user prompt 调 `callLLM`，返回规范化子任务数组（字段类型转换 + difficulty 白名单校验 + 默认值兜底；type 透传：父 epic→子 epic，否则 daily）。

### 7.6 配置示例（`server/.env.example`）
```env
LLM_PROVIDER=ollama
# LLM_API_KEY=...        # doubao 时使用
# LLM_MODEL=doubao-pro-32k
# LLM_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
# LLM_MODEL=qwen2:7b      # ollama 时使用
# LLM_BASE_URL=http://localhost:11434/v1
# PORT=3001
```

---

## 8. 视觉设计指南（已落地）

- **Art Direction**：Dark Fantasy RPG Panel — 深石墨底 + 暗紫面板层 + 金色主识别 + 紫蓝魔法强调 + 翠绿成功态
- **配色比例**：65% 深中性 / 25% 紫蓝辅助 / 10% 金色 primary
- **字体**：display 衬线游戏风（标题/数值）+ body 无衬线（正文）
- **圆角**：`rounded-md`（面板接近直角微圆）
- **装饰**：符文角饰、发光边框（box-shadow 外发光）、进度条末端辉光、任务完成粒子爆裂、技能点亮径向光芒
- **五态**：Default/Hover/Active/Focus-visible（金色发光环）/Disabled
- **语义色**：成功翠绿 `hsl(145 55% 48%)` / 警告琥珀 `hsl(30 80% 55%)` / 错误暗红 `hsl(0 55% 50%)`，饱和度与 primary 金色对齐 ±15%
- **难度色系跨页一致**：easy=success / normal=info / hard=warning / epic=accent

---

## 9. 已知问题与未实现项（如实记录）

### 9.1 阻塞性问题
- ❌ `quest-guild-electron/package.json` 为空文件（0 字节），无法安装依赖或执行 electron-builder 打包（需恢复，`package-lock.json` 可作版本参考）

### 9.2 规划中未实现
- ❌ **艾宾浩斯回顾符文**：遗忘曲线复习任务、回顾任务生成、技能节点强化光环/枯萎 — 完全未实现
- ❌ **LLM 多轮引导创建史诗任务**（4.2 模式一）：对话式引导、定义 Boss、判断序贯/并行、颗粒度拆解、属性确认 — 仅实现"一键 AI 拆解子任务"（单次调用，非多轮对话）
- ❌ **快捷录入自然语言语法**（如 `买菜 #急 30min`、`学编程>Python>列表推导`）：未实现解析器，快捷录入为表单式
- ❌ **动态预估调整（耗时系数）**：未实现同类任务预估时间修正机制
- ❌ **移动端 / 多端同步（WebDAV 等）**：未实现
- ❌ **macOS 适配 / Ollama 本地模型集成到 UI 配置 / 社区技能树分享**：未实现

### 9.3 Electron 待完善项
- ❌ **悬浮窗独立页面（`float.html`）数据双向同步**未实现 — 当前为硬编码静态 demo，未对接 electronAPI.store / 未监听 data:sync / data:storage-changed
- ❌ **单实例锁**（`requestSingleInstanceLock`）未实现 — 可多开
- ❌ **开机自启**代码被注释（`app.setLoginItemSettings`）
- ❌ **`setAsDefaultProtocolClient`** 未调用
- ❌ 根级 `assets/icon.png` 磁盘不存在（代码已回退处理），`.electron-cache` 目录不存在
- ❌ 托盘"今日任务概览"占位 disabled，未实现
- ❌ 悬浮窗展开态底部快捷栏（快速悬赏/技能树/公会商店）为占位，无 click 逻辑

### 9.4 其他
- ⚠️ electron-store 加密 key `quest-guild-2024` 为硬编码明文（安全性弱）
- ⚠️ `electronAPI.store.get` 同步调用（`sendSync`）在大数据量时可能阻塞渲染进程

---

## 10. 项目布局（如实现）

```
quest-guide/
├── src/                      # Web 前端源码
│   ├── api/                  # API 客户端（tasks/skills/shop/settings）
│   ├── components/           # AppSidebar/Topbar/FloatingWidget/Layout + ui/（shadcn 组件库）
│   ├── data/preset.ts        # 预设示例数据
│   ├── hooks/                # useTasks/useSkills/useShop/useStats/useSettings/useFloatingMode/useLocalState
│   ├── lib/                  # storage/backup/sound/chart-colors/utils
│   ├── pages/                # 6 个页面（Today/QuestPool/SkillTree/Shop/Stats/Settings）+ Example/NotFound
│   ├── types/                # quest.ts（接口）+ electron.d.ts
│   └── app.tsx / index.tsx
├── quest-guild-electron/     # Electron 桌面壳
│   ├── main.js               # 主进程（487 行）
│   ├── preload.js            # 预加载（contextBridge）
│   ├── package.json          # ⚠️ 当前为空
│   ├── package-lock.json     # 依赖锁仍在
│   ├── renderer/
│   │   ├── float.html        # 独立悬浮窗（静态原型）
│   │   └── app/              # 主窗口 Vite 构建产物
│   ├── assets/               # 图标资源目录（待用户添加）
│   ├── 安装并运行.bat / 打包成EXE.bat
│   └── .gitignore
├── server/                   # 后端服务
│   ├── index.js              # Express + SQLite（727 行，30+ 端点）
│   ├── services/llm.js       # LLM 拆解（Ollama/豆包）
│   ├── quest-guild.db        # SQLite 数据库（WAL）
│   ├── .env.example
│   └── package.json          # express + cors + better-sqlite3
├── scripts/                  # build.sh / dev.mjs / setup-git-hooks.mjs
├── shared/                   # capabilities/ static/（占位）
├── AGENTS.md                 # 开发文档
├── package.json              # 根包
└── 悬赏任务.md               # 本文档
```

---

## 11. 后续优化建议（基于当前实现差距）

| 优先级 | 待办项 |
|:---|:---|
| P0 | 恢复 `quest-guild-electron/package.json`（依据 package-lock.json），恢复打包能力 |
| P0 | 悬浮窗 `float.html` 对接 electronAPI.store + 监听 data:sync/data:storage-changed，实现与主窗口数据双向同步 |
| P1 | 实现艾宾浩斯回顾符文系统（遗忘曲线任务生成 + 技能节点强化光环/枯萎） |
| P1 | LLM 多轮对话引导创建史诗任务（替代当前单次拆解） |
| P1 | 单实例锁 + 开机自启 + 托盘今日任务概览 |
| P1 | 快捷录入自然语言语法解析器 |
| P2 | 动态预估调整（耗时系数修正同类任务预估） |
| P2 | 移动端响应式优化 / 多端同步（WebDAV 或内置同步） |
| P3 | macOS 适配 / Ollama UI 配置 / 社区技能树分享 / 自动更新 |
