# 悬赏任务公会 (Quest Guild) — 系统需求规格书 V2.0

## 1. 概述

**产品名称**：悬赏任务公会  
**核心定位**：以RPG游戏面板为视觉风格，LLM驱动任务拆解，结合注意力机制与全局技能树的轻量级目标管理工具。  
**目标用户**：希望用游戏化方式克服拖延、管理复杂长期目标，同时能处理日常零碎任务的个人用户。  
**核心价值**：把“人生目标”变成“史诗任务”，通过任务拆解引导、专注追踪、技能成长和可视化奖励，提升规划力与执行力。

---

## 2. 游戏化核心概念

| 游戏术语 | 映射现实 | 说明 |
| :--- | :--- | :--- |
| **史诗任务** | 长期/复杂目标 | 需拆解为子任务，并设定一个可校验的“任务Boss”。 |
| **每日悬赏** | 日常例行/临时小任务 | 支持定时重复或一次性，可快捷录入。 |
| **任务Boss** | 目标的最终检验成果 | 用户定义（AI可建议名称）。需手动确认“已击败”。 |
| **法力水晶** | 精力/注意力上限 | 决定可同时激活的注意力任务数量，系统根据难度建议，用户可调。 |
| **追踪任务** | 当前唯一的主线专注任务 | 同时仅1个，带计时器，实际用时与预估动态对比。 |
| **注意力任务** | 碎片时间可并行的次级任务 | 最多可激活N个（N≤法力水晶值），仅标记完成，不强制计时。 |
| **技能树** | 全局个人能力图谱 | 一棵可视化天赋树，预设框架，有拓扑关系，初期只能点亮有限方向。 |
| **公会声望** | 积分 | 完成任务获得，用于兑换自定义奖励。 |
| **回顾符文** | 艾宾浩斯遗忘曲线复习 | 按记忆规律自动生成复习任务，强化技能节点。 |

---

## 3. 平台与非功能需求

### 3.1 平台策略
- **首发平台**：Windows（桌面悬浮窗），后续适配 macOS。
- **移动端**：必须支持手机与电脑数据同步。移动端可先以响应式Web或PWA形式提供，保证查看、录入和简单操作。
- **技术栈建议**：满足轻量（安装包<50MB）、高性能、原生透明无边框窗口和跨平台潜力。

### 3.2 性能与体验
- 内存占用低，启动快（≤3秒），动画帧率稳定。
- 悬浮窗默认透明，鼠标悬停时背景毛玻璃实化，动画过渡柔和。
- 支持系统托盘常驻，支持开机自启（可配置）。

### 3.3 隐私与AI
- 接受将任务描述发送至云端大模型（OpenAI / Claude 等）进行拆解引导。
- 产品内置API Key配置界面，用户可自由选择服务商或本地模型（如Ollama），提示词可自定义。

### 3.4 同步与存储
- **本地优先**：核心数据加密存储于本地，断网可用。
- **多端同步**：提供轻量同步机制，初期可支持用户自建的WebDAV或内置轻量同步服务，确保手机与电脑任务数据一致。

---

## 4. 详细功能需求

### 4.1 PC端悬浮窗（核心交互界面）

#### 4.1.1 默认透明态
- 吸附于屏幕左上角（可拖动调整吸附边），背景完全透明。
- 展示一个微缩元素（如当前追踪任务的图标、迷你进度环或简短标题）。
- 永久置顶，但可切换“穿透模式”（不影响后方窗口操作）。

#### 4.1.2 鼠标悬停展开态
- 鼠标移入时平滑展开为圆角卡片面板，背景采用毛玻璃效果。
- 面板布局：
  - **顶部横幅**：当前追踪任务名称 + 专注计时器（时:分:秒）+ Boss进度条。
  - **中间任务区**：今日激活的注意力任务卡片流，每个卡片最多显示两级（父-子任务），可拖拽排序。
  - **底部快捷栏**：“快速悬赏”、“技能树”、“公会商店”等图标按钮。
- 鼠标移出后延时收回，可锁定展开状态。

#### 4.1.3 交互手势
- 任务卡片：左滑标记完成，右滑弹出“删除/跳过”选项。
- 面板可缩放，支持最小化到系统托盘，托盘图标可显示当前追踪任务进度。

### 4.2 任务创建与拆解（双模式）

#### 模式一：LLM多轮引导（新建史诗任务）
1. **触发**：点击面板“新建史诗任务”按钮。
2. **对话流程**（对话框形式，LLM驱动）：
   - **定义Boss**：AI提问“这个任务完成的最终标志是什么？” → 用户输入后，AI提取并建议一个Boss名称，用户确认或修改。
   - **判断结构**：AI问“是按步骤推进，还是可同时进行？” → 用户选择“序贯”或“并行”。
     - 序贯：AI生成“阶段1、阶段2...”序列，询问每个阶段的预估时间和难度。
     - 并行：AI引导输入并行分支（如“理论学习、动手实践”），并分别拆解。
   - **颗粒度拆解**：AI根据用户每天可投入的时间和希望完成天数，将阶段细化为具体子任务卡片（含预估用时）。
   - **属性确认**：AI汇总任务层级树，询问“哪些需作为追踪任务候选？”、“确认难度和标签”，并给出预估总耗时。
   - **一键生成**：用户确认后，系统创建任务层级，所有子任务存入“悬赏池”，并根据设置自动分配部分任务到今日面板。
3. **可配置性**：系统提供选项，用户可自定义引导深度和提问轮数。

#### 模式二：快捷待办录入
- **触发**：悬浮窗底部“闪电”按钮或全局快捷键（如Ctrl+Shift+Q）。
- **输入方式**：弹出极简输入条，支持**自然语言快捷语法**：
  - `买菜 #急 30min` → 生成一个30分钟临时注意力任务，标签“急”。
  - `学编程>Python>列表推导` → 按“>”解析出三级子任务并创建。
- **默认属性**：快捷任务均为“注意力任务”，无Boss，不进入史诗任务体系，可直接归档。

### 4.3 任务执行与精力分配

#### 4.3.1 悬赏池与每日激活
- 所有任务存储于后台的“悬赏池”。
- 用户每天打开面板，通过“悬赏分配”将任务拖入今日激活区。
- **强制规则**：
  - 必须且只能选择**1个追踪任务**。
  - 可同时选择**N个注意力任务**，N ≤ 当前法力水晶上限（系统根据任务难度动态建议，用户可手动调整上限值，如3~5个）。
  - 未激活的任务在今日面板**完全不可见**，需进入悬赏池查看。

#### 4.3.2 追踪任务与计时
- 激活后点击“开始专注”，计时器启动（倒计时或正计时可选）。
- 暂停需选择原因（“被打断” / “提前完成”）。
- **动态预估调整**：
  - 任务结束时，对比实际用时与预估时间。
  - 超出20%以上，系统提示并记录该任务类型的“耗时系数”，未来同类任务的预估时间将乘以该系数。
  - 完成度计算中，追踪任务权重较高，但实际用时偏差仅作为参考修正，不直接影响完成百分比。

#### 4.3.3 注意力任务
- 仅需点击“完成”标记，不记录时长。可中途放弃。
- 完成一个注意力任务获得基础积分，若连续完成当日所有注意力任务，获得额外奖励。

### 4.4 全局技能树与回顾系统

#### 4.4.1 技能树预设框架
- 提供**初始预设的技能树框架**，包含但不限于：“编程”、“设计”、“语言”、“商业”、“生活技能”等大类。
- 每个大类下有预设拓扑结构（例如“编程” → “前端/后端/数据” → 具体语言/框架）。
- **初期限制**：用户开始时拥有**有限的“技能点”**，只能点亮其中N个方向（如2个），后续通过完成任务获得技能点解锁更多节点。

#### 4.4.2 技能节点与点亮
- 节点可视化：一棵可拖拽、缩放、平移的放射状天赋树（PC和移动端适配）。
- **点亮条件**：击败某个史诗任务的Boss时，可将该任务对应的核心技能（如“Python数据分析”）添加并点亮到技能树。
- 也可手动添加已有技能节点（代表已掌握能力），并关联后续任务强化。

#### 4.4.3 艾宾浩斯回顾
- 每点亮一个新技能节点，自动按照遗忘曲线（1, 2, 4, 7, 15, 30天...）生成“回顾符文”任务。
- 回顾任务当天会出现在“每日悬赏”中，特殊标记。形式可以是：AI生成一道题、一个小练习，或用户自定义动作（如“写一个这个技能的练习片段”）。
- 完成回顾：技能节点获得“强化光环”视觉效果，并奖励额外积分。长期未复习，光环变暗甚至节点枯萎（可恢复）。

### 4.5 奖励兑换（公会商店）
- **积分来源**：完成注意力任务（+10~30）、追踪任务（+50）、击败Boss（+200~1000）、完成回顾（+15）等。
- **自定义商品**：用户可添加商品（名称、所需积分、图标），如“看一部电影（50分）”、“买新键盘（8000分）”。
- **兑换流程**：点击兑换 → 播放宝箱开启动画 → 积分扣除 → 商品进入“已兑换”清单，产生仪式感。

---

## 5. 数据需求

- **本地存储**：任务、技能树、积分、设置等均本地加密存储（SQLite或类似），确保离线可用。
- **同步数据结构**：任务层级、时间戳、状态、技能节点、积分记录等核心实体，需设计轻量同步协议（增量同步）。
- **多端冲突**：以最后修改时间戳为准，提供简单合并策略。

---

## 6. 用户体验细节补充

- **提示与成就**：完成每日所有任务、连续专注多日、点亮新技能等触发通知，但避免羞辱性语言，以游戏正向激励（如“法力充满！”）。
- **音效**：可选开关，简短清脆的完成音效或升级音效。
- **统计数据**：提供简单的个人统计面板，如“本周专注总时长”、“技能树成长轨迹”。

---

## 7. 开发优先级建议

| 优先级 | 功能模块 |
|:---|:---|
| P0（最小可用） | Windows悬浮窗基础框架、任务快捷录入与手动创建、追踪/注意力任务激活、本地存储、悬浮透明/实化交互 |
| P1（核心特色） | LLM多轮对话任务拆解、艾宾浩斯回顾、基本技能树框架（含预设）、多端同步 |
| P2（完整体验） | 全局可视化天赋树缩放交互、奖励商店、动态预估调整、更多自定义（皮肤、音效） |
| P3（扩展） | macOS适配、Ollama本地模型支持、社区技能树分享 |
